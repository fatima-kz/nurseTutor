// // /lib/store.ts (or a similar shared location)

// // This object will be instantiated only once and shared across your entire application,
// // ensuring that both the GET and POST routes are reading from and writing to the same store.
// export const sessionStore: { [key:string]: any } = {};
